<Window x:Class="TWEtaChecker.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="TW 에타 체커 — 설정"
        Width="420"
        SizeToContent="Height"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource OverlayWindowBackgroundBrush}"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        FontFamily="Malgun Gothic">
    <!-- 모달이 아니다: 팝업 위치 조정 중에 미리보기 팝업을 끌 수 있어야 한다. -->
    <StackPanel Margin="16,14,16,14">

        <!-- 팝업 -->
        <TextBlock Text="팝업" Style="{StaticResource SectionTitleStyle}"/>
        <Border Style="{StaticResource CardStyle}">
            <StackPanel>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="글자 크기" Style="{StaticResource FieldLabelStyle}"/>
                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <Button Content="−" MinWidth="30" Width="30" Padding="0" Style="{StaticResource ActionButtonStyle}" Click="FontSmaller_Click"/>
                        <TextBlock x:Name="FontSizeText" Width="44" TextAlignment="Center" FontSize="13" FontWeight="SemiBold"
                                   Foreground="{DynamicResource TextBrush}" VerticalAlignment="Center"/>
                        <Button Content="+" MinWidth="30" Width="30" Padding="0" Style="{StaticResource ActionButtonStyle}" Click="FontLarger_Click"/>
                    </StackPanel>
                </Grid>
                <Grid Margin="0,8,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="팝업 위치" Style="{StaticResource FieldLabelStyle}"/>
                    <Button Grid.Column="1" x:Name="PositionButton" Width="120" Style="{StaticResource ActionButtonStyle}" Click="Position_Click"/>
                </Grid>
                <Button Margin="0,10,0,0" Content="테스트 팝업 보기" Style="{StaticResource ActionButtonStyle}" Click="Test_Click"/>
            </StackPanel>
        </Border>

        <!-- 테일즈위버 폴더 -->
        <TextBlock Text="테일즈위버 폴더" Margin="2,14,0,6" Style="{StaticResource SectionTitleStyle}"/>
        <Border Style="{StaticResource CardStyle}">
            <StackPanel>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock x:Name="GameFolderText" Style="{StaticResource FieldValueStyle}" Margin="0,0,10,0"/>
                    <Button Grid.Column="1" Content="변경…" Style="{StaticResource ActionButtonStyle}" Click="ChangeFolder_Click"/>
                </Grid>
                <TextBlock x:Name="MessengerLogText" Margin="0,6,0,0" FontSize="11" Foreground="{DynamicResource OverlayHintTextBrush}" TextTrimming="CharacterEllipsis"/>
            </StackPanel>
        </Border>

        <!-- 에타 랭킹 -->
        <TextBlock Text="에타 랭킹" Margin="2,14,0,6" Style="{StaticResource SectionTitleStyle}"/>
        <Border Style="{StaticResource CardStyle}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock x:Name="RankingText" Style="{StaticResource FieldValueStyle}"/>
                    <TextBlock Text="매일 10시 이후 자동으로 한 번 받습니다." Margin="0,3,0,0" FontSize="11" Foreground="{DynamicResource OverlayHintTextBrush}"/>
                </StackPanel>
                <Button Grid.Column="1" x:Name="RefreshRankingButton" Content="새로 고침" Style="{StaticResource ActionButtonStyle}" Click="RefreshRanking_Click"/>
            </Grid>
        </Border>

        <!-- 버전 -->
        <TextBlock Text="버전" Margin="2,14,0,6" Style="{StaticResource SectionTitleStyle}"/>
        <Border Style="{StaticResource CardStyle}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock x:Name="VersionText" Style="{StaticResource FieldValueStyle}"/>
                    <TextBlock x:Name="UpdateStatusText" Text="새 버전은 실행할 때와 6시간마다 확인합니다." Margin="0,3,0,0" FontSize="11"
                               Foreground="{DynamicResource OverlayHintTextBrush}" TextWrapping="Wrap"/>
                </StackPanel>
                <Button Grid.Column="1" x:Name="CheckUpdateButton" Content="업데이트 확인" Style="{StaticResource ActionButtonStyle}" Click="CheckUpdate_Click"/>
            </Grid>
        </Border>

        <Grid Margin="0,14,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Button Content="설정 폴더 열기" Style="{StaticResource ActionButtonStyle}" Click="OpenConfig_Click"/>
            <Button Grid.Column="2" Content="닫기" Style="{StaticResource PrimaryButtonStyle}" Click="Close_Click"/>
        </Grid>
    </StackPanel>
</Window>
